---
const {name, url, icon} = Astro.props;
---

<a href=`${url}` target="_blank" class="flex items-center gap-2 bg-white text-black py-2 px-3 rounded-full shadow hover:bg-blue-200 transition-all">
    <img src=`${icon}` alt=`${name}` class="w-6 h-6">
    <span>{name}</span>
</a>